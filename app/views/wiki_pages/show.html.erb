<h1><%= @wiki_page.title %></h1>

<p><%= @wiki_page.content %></p>

<%= link_to 'Back to Topic', wiki_topic_path(@wiki_topic) %> |
<%= link_to 'Edit Page', edit_wiki_page_path(@wiki_page) %> |
<%= link_to 'Delete Page', wiki_page_path(@wiki_page), method: :delete, data: { confirm: 'Are you sure you want to delete this page?' } %>
