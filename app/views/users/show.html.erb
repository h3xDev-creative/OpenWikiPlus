<h1>Welcome, <%= @user.email %>!</h1>

<p>Your current settings:</p>
<ul>
  <li><strong>Theme:</strong> <%= @user.settings.last&.theme || "Not set" %></li>
</ul>

<p>Your Wiki:</p>
<% if @user.wiki %>
  <h2><%= @user.wiki.title %></h2>
  <p><%= link_to 'View Wiki', wiki_path(@user.wiki) %></p>
  <p><%= link_to 'Edit Wiki', edit_wiki_path(@user.wiki) %></p>
<% else %>
  <p>You have not created a wiki yet. <%= link_to 'Create one', new_wiki_path %></p>
<% end %>

<h3>Settings</h3>
<%= link_to 'Edit Settings', edit_settings_path(@user.settings.last) %>
<%= link_to 'Log out', destroy_user_session_path, method: :delete %>
